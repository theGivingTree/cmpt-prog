#include <iostream>
#include <algorithm>
using namespace std;
int n, m, a[100000];
int main() {
  int i, j;
  cin >> n >> m;
  int a, pa, ppa st;
  cin >> pa;
  ppa = pa;
  st = 0;
  for(i=1; i<n; ++i) {
    cin >> a;
    if(pa < a) {

    }
    else {
    }
    b=a;
  }

  for(i=0; i<m; ++i) {
    //
  }
  return 0;
}
